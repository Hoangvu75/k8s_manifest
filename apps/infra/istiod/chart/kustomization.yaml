apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
- name: istiod
  repo: https://istio-release.storage.googleapis.com/charts
  releaseName: istiod
  namespace: istio-system
  version: "1.30.2"   # phải trùng version với istio-base
  valuesFile: values.yaml
