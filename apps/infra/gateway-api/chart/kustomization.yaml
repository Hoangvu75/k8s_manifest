apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Use official NGINX Gateway Fabric installation from GitHub release
# The Helm chart approach had DNS resolution issues, using direct YAML instead
resources:
  - nginx-gateway-fabric.yaml