apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sealed-secret.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - networkpolicy-egress.yaml
  # Deployment test misconfig (Trivy sẽ báo) — xóa khi không cần test nữa
  - trivy-test-misconfig.yaml

helmCharts:
- name: helm-application
  repo: oci://registry-1.docker.io/hoangvu753
  releaseName: trivy-scan
  namespace: trivy-scan
  version: 6.16.1
  valuesFile: values.yaml
