# Secret values (merge với values.yaml khi deploy)
secret:
  enabled: true
  files:
    app-env:
      stringData:
        basicAuthPassword: "changeme"
        encryptionKey: "n8n-encryption-key-change-me"
