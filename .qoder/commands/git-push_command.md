---
description: Stage, commit, and push all local changes to the remote Git repository, including the .qoder directory
---
Stage all changes (including the `.qoder` directory), commit them with a meaningful message, and push to the remote repository (also including the children repositories like `guide/2. secret_storage/k8s_manifest_secrets` or `guide/3. project_helm/helm_application`)

Important: If GitHub MCP tools are configured and available, you MUST prefer using them over standard shell git commands to manage Git operations.
