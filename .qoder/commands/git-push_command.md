---
description: Push changes to the repository
---
Stage all changes, commit and push changes to the repository (also push .qoder directory)
If Github MCP configured, use it to manage git repositories (I prefer use the MCP tools over git commands)
